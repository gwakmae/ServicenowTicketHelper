<Window x:Class="ServiceNowTicketHelper.Views.TemplateManagerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ServiceNowTicketHelper.Views"
        mc:Ignorable="d"
        Title="템플릿 관리" Height="600" Width="800" MinHeight="550" MinWidth="700">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" MinWidth="180"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Label Grid.Row="0" Content="템플릿 목록 (Ctrl+드래그로 복사)" FontWeight="Bold"/>

            <ListBox Grid.Row="1" Margin="5" 
                     ItemsSource="{Binding Templates}" 
                     SelectedItem="{Binding SelectedTemplate}" 
                     DisplayMemberPath="TemplateName"
                     AllowDrop="True"
                     PreviewMouseLeftButtonDown="ListBox_PreviewMouseLeftButtonDown"
                     MouseMove="ListBox_MouseMove"
                     Drop="ListBox_Drop"/>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="추가" Margin="5" Padding="20,5" Command="{Binding AddCommand}"/>
                <Button Content="삭제" Margin="5" Padding="20,5" Command="{Binding DeleteCommand}"/>
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="10,0,0,0" DataContext="{Binding SelectedTemplate}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Content="선택된 템플릿 편집" FontWeight="Bold"/>

                <Label Grid.Row="1" Content="템플릿 이름:" Margin="0,10,0,0"/>
                <TextBox Grid.Row="2" Text="{Binding TemplateName, UpdateSourceTrigger=PropertyChanged}" Padding="2"/>

                <Label Grid.Row="3" Content="서비스/어플리케이션 (75% 배율 기준):" Margin="0,10,0,0"/>
                <TextBox Grid.Row="4" Text="{Binding ServiceApplication, UpdateSourceTrigger=PropertyChanged}" Padding="2"/>
                <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,5,0,0">
                    <Label Content="좌표: X="/>
                    <TextBox Width="50" Text="{Binding ServiceAppX}" IsReadOnly="True"/>
                    <Label Content="Y="/>
                    <TextBox Width="50" Text="{Binding ServiceAppY}" IsReadOnly="True"/>
                    <Button Content="좌표 찍기" Margin="10,0,0,0" Command="{Binding DataContext.CaptureServiceAppCoordCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                </StackPanel>

                <Label Grid.Row="6" Content="간단한 설명 (75% 배율 기준):" Margin="0,10,0,0"/>
                <TextBox Grid.Row="7" Text="{Binding ShortDescription, UpdateSourceTrigger=PropertyChanged}" Padding="2"/>
                <StackPanel Grid.Row="8" Orientation="Horizontal" Margin="0,5,0,0">
                    <Label Content="좌표: X="/>
                    <TextBox Width="50" Text="{Binding ShortDescX}" IsReadOnly="True"/>
                    <Label Content="Y="/>
                    <TextBox Width="50" Text="{Binding ShortDescY}" IsReadOnly="True"/>
                    <Button Content="좌표 찍기" Margin="10,0,0,0" Command="{Binding DataContext.CaptureShortDescCoordCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                </StackPanel>

                <Label Grid.Row="9" Content="상세 설명 (75% 배율 기준):" Margin="0,10,0,0"/>
                <TextBox Grid.Row="10" Text="{Binding DetailedDescription, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" MinHeight="80" Padding="2"/>
                <StackPanel Grid.Row="11" Orientation="Horizontal" Margin="0,5,0,0">
                    <Label Content="좌표: X="/>
                    <TextBox Width="50" Text="{Binding DetailDescX}" IsReadOnly="True"/>
                    <Label Content="Y="/>
                    <TextBox Width="50" Text="{Binding DetailDescY}" IsReadOnly="True"/>
                    <Button Content="좌표 찍기" Margin="10,0,0,0" Command="{Binding DataContext.CaptureDetailDescCoordCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                </StackPanel>

                <Button Grid.Row="13" Content="모든 변경사항 저장" Margin="0,20,0,0" Padding="0,10" HorizontalAlignment="Right" Command="{Binding DataContext.SaveCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>