<Window x:Class="ServiceNowTicketHelper.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ServiceNowTicketHelper.Views"
        mc:Ignorable="d"
        Title="ServiceNow 티켓 자동화 도우미" Height="350" Width="500">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="1. 실행할 템플릿을 선택하세요:" FontWeight="Bold"/>

        <ListBox Grid.Row="1" Margin="5"
                 ItemsSource="{Binding Templates}"
                 SelectedItem="{Binding SelectedTemplate}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding TemplateName}" Margin="5"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Button Grid.Row="2" Margin="5" Padding="0,10"
                Content="웹페이지에 내용 채우기"
                Command="{Binding FillTicketCommand}"/>

        <Button Grid.Row="3" Margin="5,0,5,5" Content="템플릿 관리..."
                Command="{Binding ManageTemplatesCommand}"/>

        <StatusBar Grid.Row="4" Margin="5,0,5,0">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>